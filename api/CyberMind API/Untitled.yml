info:
  name: Untitled
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:5000/api/auth/register
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
          "username": "test",
          "email": "aaa@gmail.com",
          "password": "123456"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
